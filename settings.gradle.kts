rootProject.name = "mri-studio"

// Procedure-starter templates live in their own module as real, compile-checked
// Java; the root build copies their sources into mri-studio's resources.
include("starters")
