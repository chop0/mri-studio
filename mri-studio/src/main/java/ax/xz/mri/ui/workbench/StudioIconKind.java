package ax.xz.mri.ui.workbench;

/** Semantic icon kinds for project and workbench chrome. */
public enum StudioIconKind {
    PROJECT,
    GROUP_IMPORTS,
    GROUP_SEQUENCES,
    IMPORT,
    SCENARIO,
    RUN,
    CAPTURE,
    SNAPSHOT,
    SEQUENCE,
    SIMULATION,
    OPTIMISATION_CONFIG,
    BOOKMARK,
    EIGENFIELD
}
